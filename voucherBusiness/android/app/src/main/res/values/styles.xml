<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="BootTheme" parent="Theme.AppCompat.NoActionBar">

        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/bootsplash_logo</item>
        <item name="android:windowBackground">@color/bootsplash_background</item>
        <item name="android:statusBarColor">@color/bootsplash_background</item>
        <item name="android:colorPrimaryDark">@color/bootsplash_background</item>
        <item name="android:colorPrimary">@color/bootsplash_background</item>
        <item name="android:colorAccent">@color/bootsplash_background</item>
        
    </style>
    
</resources>
